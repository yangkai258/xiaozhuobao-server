# 生产环境 — 由部署时注入或 git 仓库同名文件覆盖。
# 默认同源 /api/v1（推荐走 nginx 反代）。
# 跨域场景（例如独立 API 子域）才填绝对 URL：
#   VITE_API_BASE_URL=https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1

VITE_LOG_LEVEL=info

VITE_FEATURE_AI_HOME=true
